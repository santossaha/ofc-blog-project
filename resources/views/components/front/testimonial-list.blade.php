<div class="swiper-slide" style="width: 100%;">
    <div class="d-flex flex-column align-items-center">
        <img src="{{$image}}" alt="{{$name}}" class="rounded-circle shadow mb-4" width="128px">
        <p class="w-75 lead mt-3">{{$description}}</p>
        <hr class="w-50">
        <footer>
            <cite class="bold text-primary text-capitalize">— {{$name}},</cite> 
            <p class="small text-secondary mt-0">{{$shortTitle}}</p>
        </footer>
    </div>
</div>